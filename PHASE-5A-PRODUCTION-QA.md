# 阶段 5A：非支付生产化基线与验收

## 范围

阶段 5A 将 3J、4B、4C、4D、4E、4F 形成的开放 Skill 与外部 Agent 能力纳入可发布、
可观测、可恢复的运行底座。真实支付仍延后，不在本阶段启用。

## 已完成能力

| 领域 | 生产语义 |
| --- | --- |
| 数据库 | PostgreSQL 双库、Alembic 正式迁移、启动只校验 revision，不自动建表或注入演示数据 |
| 共享运行时 | Redis 原子限流、分布式 worker lease；生产缺少或连接失败时 fail closed |
| 文件 | 私有 S3/MinIO、临时签名下载地址、就绪探针检查 Bucket |
| 可靠投递 | Outbox 与外部 Agent Webhook 持久化回执、幂等键、重试和多实例互斥 |
| 服务边界 | 交易核心与 StaffDeck 独立入口、物理数据库、内部身份/API 契约 |
| 安全 | 多租户/API 权限、外部 Agent 凭证轮换、回调签名、防重放、SSRF/私网阻断、Skill 包扫描与沙箱限制 |
| 运维 | 健康/就绪探针、request ID、结构化日志、安全响应头、双服务 systemd 和 Nginx 分流模板 |
| 灾备 | 双数据库、对象存储、Redis 同批次备份；SHA-256 清单；隔离恢复演练与 RPO/RTO 基线 |
| 发布门禁 | 后端回归、前端构建、迁移升降级、真实依赖集成、只读压力测试和恢复演练 |

## 已执行验收

- Redis 原子窗口计数、分布式锁互斥与释放。
- PostgreSQL 从 head 逐级回滚 4F → 4E → 4D → 4C → 4B → 3J → 3H，再升级到 head。
- MinIO 私有对象上传、重复写保护、签名下载、读取和删除。
- 交易核心与 StaffDeck 双进程启动、跨库身份校验、Agent 查询和市场安装目标查询。
- 交易核心 `/api/ready` 在 PostgreSQL、Redis、MinIO 同时可用时返回 200。
- 只读压力基线：500 请求、并发 20、零失败；门禁阈值 P95 ≤ 800 ms。
- 两个 PostgreSQL 数据库和对象 Bucket 恢复到明确的测试目标；Redis RDB 完整性通过。
- 后端全量回归 1270 项通过、4 项按环境跳过、0 项失败；前端 TypeScript 生产构建通过。
- 最终一键门禁压测结果：500/500 返回 200，P95 107.99 ms，失败率 0%。

## 发布前仍需人工完成

1. 生成并保管生产 `APP_SECRET`、`INTERNAL_SERVICE_SECRET`、数据库、Redis 和对象存储密钥。
2. 在预发执行同一门禁，并进行至少一次产品双账号回归。
3. 配置云监控、日志采集、告警联系人和夜间升级路径。
4. 确认备份异地存储、加密、生命周期策略和恢复演练记录。
5. 执行法律协议、隐私政策、第三方 Skill/Agent 上架规则与安全评审。
6. 使用候选 Nginx 配置灰度切流；验证后再停用旧单体服务。

## 支付边界

保留当前演示支付以跑通订单业务，但不得把演示回调当作真实资金结果。真实支付、分账、
退款、结算、发票、对账和不可修改资金账本将在其他业务能力完成后单独进入支付接入阶段。
