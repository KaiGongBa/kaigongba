# 阶段 3H：平台争议处理、证据、调解、处理决定与申诉

## 3H-1：争议域与资金冻结

- 新增案件、证据、补件要求、时间线、调解方案、处理决定、申诉和资金操作 8 张持久化表。
- Alembic 版本 `20260731_0008` 支持升级、降级和再次升级；已使用全新 SQLite 数据库验证。
- 案件发起校验租户、订单参与方和企业负责人权限；同一订单同一时间只允许一个进行中案件。
- 发起后订单状态进入 `disputed`，结算状态进入 `frozen_dispute_demo`；任何处理决定复核前都不执行资金动作。
- 所有写操作带幂等键；关键节点同时写入争议时间线、订单事件、待办、通知与 Outbox。

## 3H-2：发起与自动证据归档

- 订单工作台新增“争议处理”页签和真实发起表单：争议类型、关联里程碑、争议金额、诉求、事实理由及举证期限。
- 发起时固化 order snapshot、agreement、quote、payment、order messages、change/cancellation、deliverable versions/acceptance records、public execution events 和 order timeline。
- 每项自动归档生成 SHA-256 摘要，后续原业务记录变化不会覆盖案件快照。
- StaffDeck 只提供订单参与方可见的执行轨迹；提示词、内部知识库、密钥、内部成本和内部审核不进入证据投影。

## 3H-3：双方回应、举证与补件

- 被申请方通过结构化表单提交首次回应，回应后进入举证阶段。
- 双方可提交文字证据或订单隔离附件；附件用途固定为 `dispute_evidence`，下载继续执行服务端 ACL。
- 证据支持“双方可见”和“仅平台可见”；另一方不能读取提交方设为仅平台可见的材料。
- 平台可向指定一方发出补件要求、设置截止时间，并同步创建高优先级待办和站内通知。
- 举证截止后普通参与方不能继续提交；案件不在举证阶段时平台也不能继续发补件要求。

## 3H-4：平台处理后台与 AI 辅助

- 管理端新增 `/enterprise/platform/disputes` 和案件详情页，支持筛选、领取、证据目录、平台文件下载、补件、调解与全过程时间线。
- AI 功能仅生成证据分类、`evidence_id` 引用、摘要哈希和缺失材料清单，不输出退款/放款建议，也不触发处理决定。
- 页面和 API 均使用“平台争议处理”“处理决定”等业务表述，不使用容易被理解为法定仲裁或司法裁判的命名。

## 3H-5：双人复核、申诉与结案

- 平台处理人可提交全额退款、部分退款、全额放款、部分放款、拆分处理或驳回诉求的决定。
- 决定提交人与复核人必须为不同平台管理员；同一管理员自行复核返回 403。
- 复核通过后只发布决定并开启申诉期，资金仍保持冻结。
- 双方可在期限内申诉，或分别明确放弃申诉；未完成申诉审核时不能结案。
- 满足结案条件后，平台再次确认才生成演示退款/放款记录并更新订单；案件、证据、决定和审计日志均为真实持久化业务数据。

## 产品协议与法律边界检查

- 已在页面、接口说明和数据模型中统一声明：平台争议处理是依据平台合作协议开展的履约纠纷处理，不等同于法定仲裁、行政裁决或司法裁判。
- 已落实明确授权、双方陈述与举证、处理决定依据、双人复核、申诉期、敏感操作二次确认和全过程审计。
- 已落实争议期间冻结结算，以及“决定复核通过不等于立即退款/放款”的资金隔离规则。
- 协议模板尚未定稿，因此正式上线真实支付前仍需由持证律师/企业法务对管辖、证据效力、电子确认、平台责任、申诉期限、资金处理授权、隐私和数据保存期限进行终签。本次交付不声称替代正式法律意见。

## 页面入口

- `/enterprise/orders/:orderId?tab=disputes`
- `/enterprise/disputes/:caseId`（甲乙双方）
- `/enterprise/platform/disputes`（平台管理员）
- `/enterprise/platform/disputes/:caseId`（平台管理员）

## 验证结果

- 3H 迁移与交易接口测试：18 项通过，其中完整争议纵向流程覆盖冻结、归档、举证隔离、平台证据下载、补件、AI 摘要、调解、双人复核、申诉、放弃申诉和演示资金结案。
- 前端测试：15 项通过。
- 前端 TypeScript 与生产构建：通过。
- 浏览器验收：订单争议页签、完整发起表单和平台争议后台可加载；导航、权限入口、字段和状态筛选正确，浏览器控制台无错误。为保护现有本地业务数据，浏览器验收未在持久库创建虚假争议案件；写入路径由独立临时数据库的端到端接口测试覆盖。
- 完整 StaffDeck 后端回归当前为 1233 通过、9 失败；3H 相关测试全部通过。失败位于既有 golden fixture/实时 socket、模型配置旧期望和单端口 MIME 日志测试，不涉及争议模块。本阶段没有修改这些失败测试来制造通过结果。

## 后续上线门槛

- 接真实支付前，将 `demo_refund` / `demo_release` 映射到持牌支付渠道，并增加渠道回执、失败补偿、对账与不可修改资金账本。
- PostgreSQL、Redis、对象存储签名 URL、病毒扫描、备份恢复和生产告警仍按技术底座计划推进。
- 协议模板定稿后，将法律版本号、适用条款版本和双方确认快照纳入案件自动归档。
