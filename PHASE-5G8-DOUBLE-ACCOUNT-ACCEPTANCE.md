# Phase 5G-8 独立双账号与外接 Agent 验收基线

## 目标

在不启用真实支付的前提下，证明开工吧交易主链可以由三个相互独立的账号完成，并证明外接 Agent 只能执行已授权任务，不能取得验收、支付、退款、放款或争议裁决权限。

## 账号与数据边界

| 参与方 | 企业 | 允许操作 | 必须拒绝的操作 |
| --- | --- | --- | --- |
| 甲方账号 | `org_demo_buyer` | 创建和发布需求、查看已发送报价、选标、确认协议、创建支付单、查看采购订单 | 打开乙方工作台、发送乙方报价、模拟支付、访问乙方组织视角 |
| 乙方账号 | `org_cloud_ops` | 查看受邀需求、查看 AI 报价草案、人工确认发送、确认乙方协议、查看交付订单 | 发布甲方需求、替甲方选标或确认、创建甲方支付单、访问甲方组织视角 |
| 平台复核员 | 无甲乙方成员关系 | 仅执行明确的演示支付复核 | 代替甲乙双方确认业务结构化动作 |
| 外接 Agent 凭据 | 绑定单个连接和能力 | Manifest、心跳、连接测试、领任务、进度事件、授权制品和结果回传 | 验收、支付、退款、放款、争议裁决及访问未授权组织数据 |

## 必须跑通的主链

1. 甲方保存并发布完整需求，平台进入匹配状态并创建受邀报价任务。
2. Worker 幂等生成乙方私有 AI 报价草案；总价和里程碑金额由交易规则确定。
3. 草案在乙方确认前对甲方返回 404，甲方也不能代替乙方发送。
4. 乙方负责人确认发送后，甲方才可比较并选择报价。
5. 甲乙双方分别确认冻结协议；任一方不能替另一方确认。
6. 甲方创建演示支付单，普通甲乙账号不能模拟支付，平台复核员完成演示回调。
7. 系统只生成一个订单，甲乙双方分别得到 buyer/provider 视角，跨企业访问返回 403。
8. 外接 Agent 完成配对、Manifest 审核、员工确认、连接测试、心跳、任务领取、进度与结果回传；网络重放不得产生重复结果。

## 发布门禁

定向验证：

```bash
./scripts/phase5g8_verify.sh --targeted
```

提交或部署前完整验证：

```bash
./scripts/phase5g8_verify.sh --full
```

门禁同时检查后端权限与状态机、开小花动态需求分析、外接 Agent SDK、前端需求与助手协议、生产构建、Ruff、diff 格式以及新增测试不得被 skip/xfail。

## 通过标准

- 所有允许操作返回成功，所有越权操作按约定返回 401、403 或 404。
- 报价、协议、支付回调、订单和 Agent 结果均满足幂等要求。
- 订单甲乙双方、成交价格、服务快照和里程碑金额保持一致。
- 外接 Agent 结果可回传，但不改变验收、支付、结算或争议状态。
- 真实支付继续关闭；本阶段不得接入或模拟真实资金渠道。
