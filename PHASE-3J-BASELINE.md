# 阶段 3J：开放式 AI 员工与 Skill 市场基线

## 不重做的既有能力

- StaffDeck 的数字员工广场、从广场复制、从空白创建及原有图标继续保留。
- 开工吧已存在 AI 服务市场、Skill 市场、发布中心、服务商入驻、平台审核、版本、安装目标和企业绑定页面。
- Marketplace 表和 Skill 安装记录均为真实数据库数据，不退回前端 mock。
- 订单已有 SOP 快照、Skill 固定版本、digest、平台审核记录和执行事件。

## 3J 需要补齐的真实缺口

1. 统一“StaffDeck 员工广场”和“开工吧可交易 AI 服务”的发布关系，但不把员工配置、提示词或知识库公开。
2. 发布版本必须绑定来源、不可变包摘要、权限清单、运行方式和安全审查结果。
3. 企业安装 Skill 后，通过 StaffDeck 内部 API 建立员工绑定；失败由 Outbox 重放，不能出现只改前端状态。
4. 订单只能引用成交时已审核的固定版本，发布者后续更新不能改变历史订单。
5. 平台托管第三方 Skill 需要独立非 root、只读根文件系统、默认禁网、临时目录和 CPU/内存/进程/时间/文件限制。
6. 在上述沙箱完成前，开放市场只允许“登记/安装元数据”和远程 API 类型，不得把不受信任代码直接放进 StaffDeck 宿主进程运行。

## 实施顺序

- 3J-1：运行环境秘密隔离和托管执行总开关。
- 3J-2：Skill 包上传、对象存储、SHA-256 校验和不可变版本。
- 3J-3：安全扫描、权限分级、平台审核与复核记录。
- 3J-4：受限执行 Worker、资源限制、禁网和 artifact 回传。
- 3J-5：AI 员工发布到服务市场的安全投影与版本关系。
- 3J-6：发布者、安装方、平台三账号 E2E 和浏览器验收。

## 完成状态

- 3J-1 已完成：Skill 子进程采用环境变量 allowlist，单次 HOME/TMP，不继承应用、数据库、模型、渠道密钥和宿主代理。
- 3J-2 已完成：发布方上传真实 ZIP；服务端计算 SHA-256，写入私有对象存储，版本相同但摘要不同会拒绝覆盖。
- 3J-3 已完成：检查 Zip Slip、符号链接、压缩炸弹、文件上限、可执行文件、硬编码密钥、未声明网络访问和危险代码信号；高风险包需两名不同管理员完成安全复核和平台终审。
- 3J-4 已完成：提供默认关闭的独立容器执行 Worker。启用后强制非 root、只读根文件系统、禁网、cap-drop、no-new-privileges、CPU/内存/PID/超时/tmpfs 限制，并把输出制品写回对象存储。生产镜像必须固定 sha256 digest。
- 3J-5 已完成：市场 Skill 版本显式绑定审核后的固定包 ID 和 digest；安装到 StaffDeck 的绑定只传安全投影和固定版本标识，不传提示词、知识库、密钥或内部成本。AI 员工商业服务继续通过 StaffDeck 内部安全投影发布。
- 3J-6 已完成：发布者、独立安全审核员、平台终审员的自动化回归通过；市场安装与 Outbox 重放回归通过；前端发布页、真实包上传扫描状态和平台包审核页已完成构建验证。

## 当前生产策略

- `HOSTED_SKILL_EXECUTION_ENABLED=false`，开放市场默认使用“外部 Agent”或“远程 API”。
- 开启平台托管并不只改一个前端开关：必须先部署隔离 Worker、固定 digest 镜像、配置运行节点权限，并完成安全和压力验收。
- 历史元数据登记接口保留兼容，但预发/生产发布“平台托管”商品时必须绑定真实上传并审核通过的包版本。
