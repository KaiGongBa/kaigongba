# 阶段 5C 验收报告

验收日期：2026-08-02

## 结论

阶段 5C 通过本地候选版、完整基础设施和真实多账号浏览器验收，可以作为后续预发布部署的候选提交。
本阶段没有接入真实支付，也没有用演示前端替代真实业务数据；支付渠道保持 `demo`，需求、报价、协议、订单、交付、争议和执行记录均读取现有持久化业务数据。

## 自动化门禁

| 门禁 | 结果 |
| --- | --- |
| 前端 Vitest | 20 个文件、92 项通过 |
| 前端生产构建 | 通过 |
| i18n 覆盖 | 4538 项英文翻译通过 |
| Vite 配置检查 | 通过 |
| 后端 Ruff | 通过 |
| 后端 pytest | 1277 项通过、4 项既有环境跳过 |
| 新增 skip / xfail 防线 | 通过，无新增跳过 |
| 5C 发布资产契约 | 4 项通过 |
| 只读双账号烟测 | 19 项通过 |
| PostgreSQL 迁移/回滚/重升 | 通过，当前 head 为 `20260801_0014` |
| Redis 共享运行时 | 通过 |
| MinIO 私有对象存储 | 通过 |
| 交易核心 / StaffDeck 分离 | 通过 |
| 只读压力门禁 | 500 次请求、20 并发、0 失败 |
| 隔离备份恢复演练 | 通过 |

执行命令：

```bash
./scripts/phase5c_verify.sh --local
./scripts/phase5c_verify.sh --full-infra
```

生成物位于 `.artifacts/phase5c/`；报告不保存密码或 Token。

## 浏览器业务验收

| 角色 / 范围 | 验收内容 | 结果 |
| --- | --- | --- |
| 甲方 | 我的需求、需求详情、报价比较、合作协议、订单工作区、用户侧争议详情 | 通过 |
| 乙方 | 报价管理、服务商工作台、同一订单服务方视角、用户侧争议详情 | 通过 |
| 平台管理员 | 平台争议详情、登录账号、企业与团队、AI 员工市场、Skill 市场 | 通过 |
| 普通成员 | `/enterprise/accounts` 转到企业与团队；无邀请成员入口；平台争议后台转回订单页 | 通过 |
| 项目中心 | 按员工查看、按项目查看、真实项目卡片进入原订单详情 | 通过 |
| 订单工作区 | 8 个页签完整；材料、执行记录切换；浏览器返回和深链刷新保持页签 | 通过 |
| 开小花 | 在 Skill 市场原页面打开独立抽屉；URL 不改变；Esc 关闭并恢复入口焦点 | 通过 |
| 服务恢复 | 后端和 Vite 重启后，登录会话、企业选择、订单深链和 19 项只读烟测恢复 | 通过 |
| 响应式 | 640×360（1280×720 的 200% 等效）、1280×720、1440×900 | 通过 |

浏览器验收使用现有真实验收订单 `order_7d75df0f851d45db`、需求 `req_a6fe8eeaf4d2476d`、协议 `agreement_c0dd629cef4845c2` 和争议 `dispute_5c28a57b77f34dd4`，未创建或修改业务记录。

## 本轮发现并修复

1. 发布清单保留 Git porcelain 首行空格，`.github/...` 不再被错误记录为 `github/...`。
2. 只读烟测实际读取支付单并断言 `channel=demo`，不再只在报告中声明支付边界。
3. 对话端共用侧栏在窄屏临时自动收起，返回桌面宽度后恢复用户原有展开偏好。
4. 订单面包屑固定标题宽度并截断长订单号，200% 等效布局不再把“我的订单”挤成竖排。

## 非阻塞项与边界

- 真实支付仍明确关闭，待其他功能完成后单独接入和验收。
- 构建仍有既有主包大于 500 kB 的警告，不阻塞 5C；后续可单列代码分包优化。
- 后端存在第三方依赖弃用警告和 4 项既有环境跳过；5C 防线已保证本轮没有新增 skip/xfail。
- 本报告只证明本地候选版及完整基础设施门禁通过；推送、PR 和预发布部署仍需单独执行。
